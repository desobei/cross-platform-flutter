import 'package:drift/drift.dart';

QueryExecutor openCarStoreConnection() {
  throw UnsupportedError('Drift is not supported on this platform.');
}

QueryExecutor openCarStoreMemoryConnection() {
  throw UnsupportedError('In-memory Drift is not supported on this platform.');
}
